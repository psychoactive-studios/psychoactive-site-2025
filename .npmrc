; Skip strict optional-peer-dependency checks during install.
; @vercel/analytics declares @sveltejs/kit as an optional peer, which
; transitively wants Vite 8 — conflicts with Nuxt 4's Vite 7. Since we
; don't use Svelte, the conflict is irrelevant but npm blocks installs
; unless this flag is set.
legacy-peer-deps=true
